# EasyMenu 项目技术文档

## 项目概述

EasyMenu 是一个基于 Android 平台的菜单管理应用程序，采用 Java 语言开发，实现了完整的菜单管理功能，包括用户登录、菜单增删改查、搜索、批量操作等功能。

## 核心功能特性

### 1. 闪屏功能

**实现类：** `SplashActivity.java`

**功能描述：**
- 应用启动时展示闪屏页面，显示应用Logo和欢迎界面
- 自动检测用户登录状态，智能跳转
  - 已登录用户 → 跳转到菜单列表页
  - 未登录用户 → 跳转到登录页

**技术实现：**
- 使用 `Handler` 实现延迟跳转
- 闪屏时间控制：最少显示 2 秒，最多 5 秒
- 异步查询数据库避免阻塞主线程
- 超时保护机制，防止数据库操作卡死
- 使用 `volatile` 和标志位防止多次跳转

**代码关键点：**
```java
// 检查登录状态并跳转
private void checkLoginStatusAndNavigate() {
    AppExecutors.runDb(() -> {
        User loggedUser = userDao.getLoggedInUser();
        // 计算延迟时间，确保至少显示2秒
        AppExecutors.runMain(() -> {
            handler.postDelayed(() -> navigateToNext(loggedUser), finalDelay);
        });
    });
}
```

---

### 2. 注册功能

**实现类：** `RegisterActivity.java`、`RegisterPresenter.java`

**功能描述：**
- 新用户注册账号
- 输入用户名、密码和确认密码
- 用户名唯一性校验
- 密码加密存储

**技术实现：**
- MVP设计模式：View层（RegisterActivity）+ Presenter层（RegisterPresenter）
- 使用 SQLite 数据库存储用户信息（users表）
- 密码使用 `PasswordUtils` 工具类进行加密
- 表单验证：用户名、密码、确认密码不能为空，密码必须一致

**数据库设计：**
```sql
CREATE TABLE users (
    user_id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE,
    password TEXT,
    login_status INTEGER DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

### 3. 登录功能

**实现类：** `LoginActivity.java`、`LoginPresenter.java`、`UserDao.java`

**功能描述：**
- 用户输入用户名和密码进行登录验证
- 登录成功后更新登录状态
- 记录登录历史
- 支持选择历史登录用户名

**技术实现：**
- MVP设计模式：View层（LoginActivity）+ Presenter层（LoginPresenter）
- 使用 SQLite 数据库存储用户信息和登录历史
- 登录成功后将 users 表的 `login_status` 字段设置为 1
- 每次登录成功在 `login_history` 表中记录一条登录历史
- 使用 Spinner 显示历史登录用户名，支持快速选择

**关键代码：**
```java
// 登录成功处理
if (ok) {
    // 设置登录状态
    userDao.setLoggedInUser(uName);
    // 记录登录历史
    historyDao.insertHistory(uName, DateTimeUtils.nowDateTimeString());
}
```

---

### 4. 退出登录功能

**实现类：** `MenuListActivity.java`、`MenuPresenter.java`、`UserDao.java`

**功能描述：**
- 在菜单列表页面点击退出登录按钮
- 清除当前用户的登录状态
- 返回登录页面

**技术实现：**
- 点击退出按钮调用 `presenter.onLogoutClicked(username)`
- Presenter 调用 `UserDao.setLogout(username)` 清除登录状态
- 将 users 表中对应用户的 `login_status` 字段设置为 0
- 返回到 LoginActivity

**关键代码：**
```java
// 退出登录
public void onLogoutClicked(String username) {
    AppExecutors.runDb(() -> {
        userDao.setLogout(username); // 设置 login_status=0
        AppExecutors.runMain(() -> {
            view.backToLogin(); // 返回登录页
        });
    });
}
```

---

### 5. 菜单添加/修改功能

**实现类：** 
- View层：`MenuListActivity.java`、`MenuEditActivity.java`
- Presenter层：`MenuPresenter.java`
- Model层：`MenuDao.java`、`Menu.java`

**功能描述：**
- **添加菜单：** 在菜单列表页点击"添加"按钮，启动 `MenuEditActivity` 进行菜单添加
- **修改菜单：** 点击菜单项或长按选择"编辑"，启动 `MenuEditActivity` 进行菜单编辑
- 表单包含4个必填字段：菜名、分类、价格、描述
- 保存成功后返回列表页并刷新数据

**技术实现：**
- **启动另一个界面实现添加/修改：**
  - 使用 `Intent` 启动 `MenuEditActivity`
  - 通过 `mode` 参数区分"add"和"edit"模式
  - 编辑模式下通过 `menu_id` 参数传递要编辑的菜单ID
  - 使用 `ActivityResultLauncher` 处理编辑结果回调

**菜单信息字段（不少于4个）：**
| 字段名 | 类型 | 说明 |
|--------|------|------|
| menu_id | INTEGER | 菜单ID（主键） |
| name | TEXT | 菜名 |
| category | TEXT | 分类 |
| price | REAL | 价格 |
| description | TEXT | 描述 |
| created_at | DATETIME | 创建时间 |

**关键代码：**
```java
// 跳转到添加页面
public void navigateToAddMenu() {
    Intent it = new Intent(this, MenuEditActivity.class);
    it.putExtra("mode", "add");
    editLauncher.launch(it);
}

// 跳转到编辑页面
public void navigateToEditMenu(int menuId) {
    Intent it = new Intent(this, MenuEditActivity.class);
    it.putExtra("mode", "edit");
    it.putExtra("menu_id", menuId);
    editLauncher.launch(it);
}
```

---

### 6. 菜单列表展示

**实现类：** `MenuListActivity.java`、`MenuListAdapter.java`、`ListView`

**功能描述：**
- 使用 ListView 展示所有菜单信息
- 显示菜名、分类、价格、描述
- 支持按菜名和分类搜索
- 支持空状态提示

**技术实现：**
- **适配器+ListView：**
  - 使用 `MenuListAdapter` 自定义适配器
  - 继承 `BaseAdapter` 实现数据绑定
  - 使用 `ViewHolder` 模式优化 ListView 性能
  - 支持普通展示模式和批量选择模式

**ListView 项布局：**
```xml
<LinearLayout>
    <TextView android:id="@+id/tvName" />         <!-- 菜名 -->
    <TextView android:id="@+id/tvCategory" />     <!-- 分类 -->
    <TextView android:id="@+id/tvPrice" />        <!-- 价格 -->
    <TextView android:id="@+id/tvDesc" />         <!-- 描述 -->
    <CheckBox android:id="@+id/checkboxSelection" /> <!-- 多选复选框 -->
</LinearLayout>
```

---

### 7. 菜单删除功能

**实现类：** `MenuListActivity.java`、`MenuPresenter.java`、`MenuDao.java`

**功能描述：**
- 长按菜单项显示操作菜单
- 支持单个删除
- 支持批量删除（多选模式）

**技术实现：**
- **单个删除：**
  - 长按菜单项 → 显示操作菜单 → 选择"删除" → 确认对话框 → 执行删除
  - 调用 `MenuDao.deleteMenuById(menuId)` 删除单个菜单

- **批量删除：**
  - 长按菜单项 → 显示操作菜单 → 选择"多选" → 进入多选模式
  - 点击菜单项进行多选
  - 点击"批量删除"按钮 → 确认对话框 → 执行批量删除
  - 调用 `MenuDao.batchDeleteMenus(menuIds)` 批量删除
  - 使用数据库事务确保数据一致性

**关键代码：**
```java
// 批量删除
public int batchDeleteMenus(List<Integer> menuIds) {
    db.beginTransaction();
    try {
        for (Integer id : menuIds) {
            totalDeleted += db.delete("menus", "menu_id=?",
                    new String[]{String.valueOf(id)});
        }
        db.setTransactionSuccessful();
    } finally {
        db.endTransaction();
    }
    return totalDeleted;
}
```

---

### 8. 本地SQLite数据库存储

**实现类：** `DatabaseHelper.java`、`MenuDao.java`、`UserDao.java`、`LoginHistoryDao.java`

**数据库设计：**

**数据库名：** EasyMenu.db
**数据库版本：** 2

**数据表结构：**

1. **users表（用户表）**
   ```sql
   CREATE TABLE users (
       user_id INTEGER PRIMARY KEY AUTOINCREMENT,
       username TEXT UNIQUE,
       password TEXT,
       login_status INTEGER DEFAULT 0,
       created_at DATETIME DEFAULT CURRENT_TIMESTAMP
   );
   ```

2. **menus表（菜单表）**
   ```sql
   CREATE TABLE menus (
       menu_id INTEGER PRIMARY KEY AUTOINCREMENT,
       name TEXT,
       category TEXT,
       price REAL,
       description TEXT,
       created_at DATETIME DEFAULT CURRENT_TIMESTAMP
   );
   ```

3. **login_history表（登录历史表）**
   ```sql
   CREATE TABLE login_history (
       history_id INTEGER PRIMARY KEY AUTOINCREMENT,
       username TEXT,
       login_time DATETIME,
       device_info TEXT
   );
   ```

**初始数据（记录不少于10条）：**
- 宫保鸡丁（热菜，38.0元）
- 鱼香肉丝（热菜，28.0元）
- 麻婆豆腐（热菜，18.0元）
- 回锅肉（热菜，35.0元）
- 水煮牛肉（热菜，48.0元）
- 糖醋排骨（热菜，45.0元）
- 清炒时蔬（素菜，16.0元）
- 番茄鸡蛋汤（汤类，12.0元）
- 扬州炒饭（主食，22.0元）
- 担担面（主食，10.0元）

**数据库特性：**
- 使用单例模式确保数据库实例唯一
- 线程安全：使用 `synchronized` 和 `volatile` 保证并发安全
- 支持数据库版本升级（增量升级，保留用户数据）
- 创建索引提升查询性能
- 使用事务保证批量操作的原子性

---

### 9. 记录多个曾登录过的用户名

**实现类：** `LoginActivity.java`、`LoginHistoryDao.java`

**功能描述：**
- 每次登录成功后，在 `login_history` 表中记录用户名和登录时间
- 登录页面显示历史登录用户名下拉列表（Spinner）
- 支持快速选择历史用户名

**技术实现：**
- **记录登录历史：**
  ```java
  // 登录成功后记录
  historyDao.insertHistory(uName, DateTimeUtils.nowDateTimeString());
  ```

- **查询历史用户名：**
  ```sql
  SELECT username, MAX(login_time) AS last_time
  FROM login_history
  WHERE username IS NOT NULL AND username <> ?
  GROUP BY username
  ORDER BY last_time DESC
  ```

- **UI展示：**
  - 使用 Spinner 组件展示历史用户名列表
  - 选择历史用户名后自动填充到用户名输入框
  - 异步加载数据，避免阻塞UI线程

**关键代码：**
```java
// 初始化历史用户名下拉框
private void initHistoryUsersSpinner() {
    AppExecutors.runDb(() -> {
        List<String> usernames = historyDao.getDistinctUsernames();
        AppExecutors.runMain(() -> {
            historyItems.clear();
            historyItems.add(getString(R.string.hint_select_history_user));
            if (usernames != null) historyItems.addAll(usernames);
            historyAdapter.notifyDataSetChanged();
        });
    });
}
```

---

## MVP设计模式

### 架构分层

```
┌─────────────────────────────────────────────────────────┐
│                     View层（视图层）                     │
│  - SplashActivity、LoginActivity、RegisterActivity      │
│  - MenuListActivity、MenuEditActivity                   │
│  - MenuListAdapter（适配器）                             │
└─────────────────────────────────────────────────────────┘
                           ↕ (通过接口通信)
┌─────────────────────────────────────────────────────────┐
│                  Contract层（契约层）                    │
│  - LoginContract（登录模块契约）                         │
│  - RegisterContract（注册模块契约）                      │
│  - MenuContract（菜单模块契约）                          │
└─────────────────────────────────────────────────────────┘
                           ↕ (通过接口通信)
┌─────────────────────────────────────────────────────────┐
│                Presenter层（业务逻辑层）                 │
│  - LoginPresenter（登录业务逻辑）                        │
│  - RegisterPresenter（注册业务逻辑）                     │
│  - MenuPresenter（菜单业务逻辑）                         │
└─────────────────────────────────────────────────────────┘
                           ↕ (调用Model层)
┌─────────────────────────────────────────────────────────┐
│                   Model层（数据层）                      │
│  - MenuDao、UserDao、LoginHistoryDao                    │
│  - Menu、User、LoginHistory（实体类）                    │
│  - DatabaseHelper（数据库帮助类）                         │
└─────────────────────────────────────────────────────────┘
```

### MVP优势

1. **职责分离：**
   - View层：负责UI展示和用户交互
   - Presenter层：负责业务逻辑处理
   - Model层：负责数据持久化和业务数据

2. **低耦合：**
   - View和Model不直接交互，通过Presenter进行通信
   - 通过Contract接口定义契约，实现解耦

3. **可测试性：**
   - Presenter层不依赖Android组件，便于单元测试
   - View接口可以Mock，便于测试

4. **可维护性：**
   - 代码结构清晰，易于理解和维护
   - 各层独立修改，不会影响其他层

### Contract接口示例

```java
// 登录模块契约
public interface LoginContract {
    // View接口：定义View层的UI操作
    interface View {
        void showLoading(boolean show);
        void showMessage(String msg);
        void navigateToMain(String username);
        void navigateToRegister();
    }
    
    // Presenter接口：定义Presenter层的业务逻辑
    interface Presenter {
        void onLoginClicked(String username, String password);
        void onRegisterLinkClicked();
        void detach();
    }
}
```

---

## 项目目录结构

```
app/src/main/java/cn/edu/cqust/easymenu/
├── contract/                    # 契约接口层（MVP）
│   ├── LoginContract.java       # 登录模块契约
│   ├── RegisterContract.java    # 注册模块契约
│   └── MenuContract.java        # 菜单模块契约
├── model/                       # Model层（数据层）
│   ├── MenuDao.java             # 菜单数据访问对象
│   ├── UserDao.java             # 用户数据访问对象
│   ├── LoginHistoryDao.java     # 登录历史数据访问对象
│   ├── Menu.java                # 菜单实体类
│   ├── User.java                # 用户实体类
│   └── LoginHistory.java        # 登录历史实体类
├── presenter/                   # Presenter层（业务逻辑层）
│   ├── LoginPresenter.java      # 登录业务逻辑
│   ├── RegisterPresenter.java   # 注册业务逻辑
│   └── MenuPresenter.java       # 菜单业务逻辑
├── view/                        # View层（视图层）
│   ├── SplashActivity.java      # 闪屏页面
│   ├── LoginActivity.java       # 登录页面
│   ├── RegisterActivity.java    # 注册页面
│   ├── MenuListActivity.java    # 菜单列表页面
│   ├── MenuEditActivity.java    # 菜单编辑页面
│   └── adapter/                # 适配器
│       └── MenuListAdapter.java # 菜单列表适配器
├── utils/                       # 工具类
│   ├── AppExecutors.java       # 线程池管理
│   ├── DateTimeUtils.java      # 日期时间工具
│   ├── PasswordUtils.java      # 密码加密工具
│   ├── StringUtils.java        # 字符串工具
│   └── InsetUtils.java         # 系统栏工具
├── DatabaseHelper.java         # 数据库帮助类
└── EasyMenuApplication.java    # Application类
```

---

## 技术要点总结

### 1. 闪屏功能
- ✅ 使用 Handler 实现延迟跳转
- ✅ 检查登录状态，智能跳转
- ✅ 时间控制：最少2秒，最多5秒
- ✅ 超时保护机制

### 2. 注册功能
- ✅ 用户名唯一性校验
- ✅ 密码加密存储
- ✅ 表单验证
- ✅ 注册成功返回登录页

### 3. 登录功能
- ✅ 用户名密码验证
- ✅ 登录状态管理
- ✅ 密码加密验证
- ✅ 登录成功记录历史

### 4. 退出登录功能
- ✅ 清除登录状态
- ✅ 返回登录页面
- ✅ 数据库状态更新

### 5. 菜单添加/修改功能
- ✅ 启动另一个界面实现
- ✅ 使用 Intent 传递参数
- ✅ 4个必填字段
- ✅ 表单验证
- ✅ ActivityResultLauncher 回调

### 6. 菜单信息字段
- ✅ menu_id（主键）
- ✅ name（菜名）
- ✅ category（分类）
- ✅ price（价格）
- ✅ description（描述）
- ✅ created_at（创建时间）

### 7. 记录不少于10条
- ✅ 初始化10条菜单数据
- ✅ 包含不同分类（热菜、素菜、汤类、主食）

### 8. 记录多个曾登录过的用户名
- ✅ login_history表记录登录历史
- ✅ Spinner展示历史用户名
- ✅ 支持快速选择
- ✅ 按最近登录时间排序

### 9. 本地SQLite数据库存储
- ✅ users表（用户信息）
- ✅ menus表（菜单信息）
- ✅ login_history表（登录历史）
- ✅ 索引优化查询性能
- ✅ 事务保证数据一致性

### 10. MVP设计模式
- ✅ View层（UI展示）
- ✅ Presenter层（业务逻辑）
- ✅ Model层（数据访问）
- ✅ Contract接口（契约定义）
- ✅ 职责分离、低耦合

### 11. 适配器+ListView组件
- ✅ MenuListAdapter自定义适配器
- ✅ BaseAdapter实现数据绑定
- ✅ ViewHolder优化性能
- ✅ 支持普通模式和多选模式

---

## 编译和运行

### 环境要求
- JDK 17 或更高
- Android Studio Arctic Fox 或更高
- Android SDK API 21+ (Android 5.0+)

### 编译步骤

1. 使用 Android Studio 打开项目
2. 等待 Gradle 同步完成
3. 连接 Android 设备或启动模拟器
4. 点击 Run 运行应用

或使用命令行：
```bash
./gradlew assembleDebug
```

---

## 总结

本项目完整实现了所有要求的功能：
1. ✅ 闪屏功能
2. ✅ 注册功能
3. ✅ 登录功能
4. ✅ 退出登录功能
5. ✅ 执行添加或修改功能时，启动另一个界面实现
6. ✅ 菜单信息字段不少于4个
7. ✅ 记录不少于10条
8. ✅ 能记录多个曾登录过的用户名
9. ✅ 采用本地SQLite数据库保存用户名和菜单信息
10. ✅ 采用MVP的设计模式
11. ✅ 使用适配器+ListView组件展示菜单信息

项目结构清晰，代码注释完整，便于理解和维护。
